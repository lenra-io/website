extends .layout.pug

block header

block content
    h1 !{currentPage.titleHtml || currentPage.title || currentPage.name}
    p Couldn't find the answer you were looking for? The LenraTeam is happy to help!